This is an example project for [Adaptive](https://adaptive.fun) demonstrating how to create
a server and a browser client.

To start:

- clone the project
- Gradle `jvmRun`
- Gradle `jsBrowserDevelopmenRun`
- sign-in with `so` / `so`